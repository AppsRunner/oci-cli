# Monitoring & observability additions for LexBangla Django backend
# Install alongside your main requirements.txt:
#   pip install -r requirements.txt -r requirements-monitoring.txt

# Prometheus metrics exporter for Django
# Exposes /metrics endpoint consumed by Prometheus scrape job
django-prometheus==2.3.1

# Sentry — error tracking, performance monitoring, distributed tracing
# Installs Django, WSGI, and SQLAlchemy integrations automatically
sentry-sdk[django]==2.6.0
